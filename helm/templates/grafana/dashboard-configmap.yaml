{{- if .Values.grafana.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vantage-exporter.fullname" . }}-dashboards
  labels:
    {{- include "vantage-exporter.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  vantage-overview.json: |-
{{ .Files.Get "dashboards/compare-skils.json" | indent 4 }}
{{- end }}